# Apiserver egress for the Argo executor sidecar comes from the
# training-allow-apiserver CiliumNetworkPolicy (selects all pods in this
# namespace) — SA labels are invisible to Cilium endpoint selectors.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: training-workflow
  namespace: training
