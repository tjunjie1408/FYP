apiVersion: v1
kind: ServiceAccount
metadata:
  name: training-workflow
  namespace: training
  labels:
    # CCNP allows kube-apiserver egress for the Argo executor sidecar.
    fyp.io/needs-api: "true"
